# 🏗️ SmartLocator Framework - Complete Architecture

**Visual reference for the enhanced framework architecture**

---

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                        SmartLocator Framework                        │
│                    (AI-Powered Test Automation)                      │
└─────────────────────────────────────────────────────────────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────┐          ┌───────────────┐          ┌──────────────┐
│  Test Layer   │          │   POM Layer   │          │  AI Layer    │
│               │          │               │          │              │
│  • Test Cases │◀────────▶│  SmartPage    │◀────────▶│  AI Healer   │
│  • Fixtures   │          │  SmartLocator │          │  AI Gateway  │
│  • Assertions │          │  Adapters     │          │  Providers   │
└───────────────┘          └───────────────┘          └──────────────┘
                                    │
        ┌───────────────────────────┼───────────────────────────┐
        │                           │                           │
        ▼                           ▼                           ▼
┌───────────────┐          ┌───────────────┐          ┌──────────────┐
│  Playwright   │          │   Selenium    │          │  REST API    │
│   Browser     │          │   WebDriver   │          │  Services    │
└───────────────┘          └───────────────┘          └──────────────┘
```

---

## 🎯 SmartLocator Component Architecture

```
┌───────────────────────────────────────────────────────────────────────┐
│                          SmartLocator                                  │
│                   (Self-Healing Locator Wrapper)                       │
├───────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Core Properties:                                                       │
│  • locator: str (current working locator)                              │
│  • adapter: FrameworkAdapter (Playwright or Selenium)                  │
│  • context_hint: str (human-readable description)                      │
│  • ai_healer: AIHealer (automatic repair engine)                       │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                  Basic Interactions (7)                         │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  click()          │  fill(text)     │  text()                  │  │
│  │  is_visible()     │  wait()         │  get_current_locator()   │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                  Form Controls (8)                              │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  check()          │  uncheck()       │  is_checked()           │  │
│  │  select_option()  │  get_selected()  │  upload_file()          │  │
│  │  get_value()      │  is_enabled()                              │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                     Actions (7)                                 │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  hover()          │  double_click()  │  right_click()          │  │
│  │  drag_to()        │  scroll_into_view()                        │  │
│  │  press_key()      │  count()                                   │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                 Visibility & Waits (4)                          │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  wait_visible(timeout)    │  wait_hidden(timeout)              │  │
│  │  is_visible()             │  is_enabled()                      │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │                    Attributes (3)                               │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  get_attribute(name) │ get_property(name) │ get_value()        │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │             AI Auto-Healing (All Methods)                       │  │
│  ├────────────────────────────────────────────────────────────────┤  │
│  │  _execute_with_healing()                                        │  │
│  │  • Try current locator                                          │  │
│  │  • On failure, call AI healer                                   │  │
│  │  • Get repaired locator                                         │  │
│  │  • Retry with new locator                                       │  │
│  │  • Update locator if successful                                 │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                         │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 🔧 FrameworkAdapter Architecture

```
┌───────────────────────────────────────────────────────────────────────┐
│                      FrameworkAdapter (ABC)                            │
│                    (Framework-Agnostic Interface)                      │
├───────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Abstract Methods (40+):                                                │
│                                                                         │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌───────────────┐ │
│  │   Basic (7)         │  │  Form Controls (8)  │  │  Actions (7)  │ │
│  ├─────────────────────┤  ├─────────────────────┤  ├───────────────┤ │
│  │ • find_element()    │  │ • check_checkbox()  │  │ • hover()     │ │
│  │ • click()           │  │ • uncheck_checkbox()│  │ • dbl_click() │ │
│  │ • fill()            │  │ • is_checked()      │  │ • right_click│ │
│  │ • text()            │  │ • select_radio()    │  │ • drag_drop() │ │
│  │ • is_visible()      │  │ • select_dropdown() │  │ • scroll()    │ │
│  │ • wait()            │  │ • get_selected()    │  │ • press_key() │ │
│  │ • get_locator()     │  │ • upload_file()     │  │               │ │
│  └─────────────────────┘  └─────────────────────┘  └───────────────┘ │
│                                                                         │
│  ┌─────────────────────┐  ┌─────────────────────┐  ┌───────────────┐ │
│  │  Visibility (4)     │  │  Attributes (3)     │  │ Containers(2) │ │
│  ├─────────────────────┤  ├─────────────────────┤  ├───────────────┤ │
│  │ • wait_visible()    │  │ • get_attribute()   │  │ • is_modal()  │ │
│  │ • wait_hidden()     │  │ • get_property()    │  │ • close_modal│ │
│  │ • is_enabled()      │  │ • get_value()       │  │               │ │
│  └─────────────────────┘  └─────────────────────┘  └───────────────┘ │
│                                                                         │
│  ┌─────────────────────┐  ┌─────────────────────────────────────────┐ │
│  │   Tables (3)        │  │      Multi-Element (2)                  │ │
│  ├─────────────────────┤  ├─────────────────────────────────────────┤ │
│  │ • get_table_data()  │  │ • find_elements()                       │ │
│  │ • get_table_row()   │  │ • get_element_count()                   │ │
│  │ • get_table_cell()  │  │                                         │ │
│  └─────────────────────┘  └─────────────────────────────────────────┘ │
│                                                                         │
└────────────────────────────────┬──────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
    ┌───────────────────────┐   ┌───────────────────────┐
    │  PlaywrightAdapter    │   │   SeleniumAdapter     │
    │     (250+ lines)      │   │     (400+ lines)      │
    ├───────────────────────┤   ├───────────────────────┤
    │                       │   │                       │
    │  Playwright API:      │   │  Selenium API:        │
    │  • .check()           │   │  • ActionChains       │
    │  • .hover()           │   │  • WebDriverWait      │
    │  • .dblclick()        │   │  • Select class       │
    │  • .drag_to()         │   │  • Keys enum          │
    │  • .wait_for()        │   │  • expected_conditions│
    │  • .select_option()   │   │  • .is_selected()     │
    │  • .set_input_files() │   │  • .send_keys()       │
    │  • .all()             │   │  • .find_elements()   │
    │                       │   │                       │
    └───────────────────────┘   └───────────────────────┘
```

---

## 🤖 AI Healing Architecture

```
┌───────────────────────────────────────────────────────────────────────┐
│                          AI Healing Flow                               │
└───────────────────────────────────────────────────────────────────────┘
                                    │
                    1. Action Called (e.g., click())
                                    │
                                    ▼
        ┌────────────────────────────────────────────────────┐
        │  SmartLocator._execute_with_healing()              │
        └────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
            2. Try Current Locator            3. Catch Exception
                    │                               │
                    ▼                               ▼
        ┌─────────────────────┐         ┌─────────────────────┐
        │  Success            │         │  Failure            │
        │  Return Result      │         │  Call AI Healer     │
        └─────────────────────┘         └─────────────────────┘
                                                    │
                                    4. AI Analysis
                                                    │
                        ┌───────────────────────────┴───────────────────────┐
                        │              AIHealer.repair_locator()             │
                        ├────────────────────────────────────────────────────┤
                        │  • Get page HTML                                   │
                        │  • Extract context (nearby elements)               │
                        │  • Build prompt with context_hint                  │
                        │  • Call AI Gateway (Groq/OpenRouter/etc.)         │
                        │  • Parse AI response                               │
                        │  • Extract new locator strategies                  │
                        └────────────────────────────────────────────────────┘
                                                    │
                                    5. Try AI Suggestions
                                                    │
                        ┌───────────────────────────┴───────────────────────┐
                        │                                                    │
                        ▼                                                    ▼
            ┌─────────────────────┐                            ┌─────────────────────┐
            │  Success            │                            │  All Failed         │
            │  • Update locator   │                            │  • Raise Exception  │
            │  • Log change       │                            │  • Report failure   │
            │  • Return result    │                            │                     │
            └─────────────────────┘                            └─────────────────────┘
```

---

## 🌐 Multi-Framework Support

```
┌───────────────────────────────────────────────────────────────────────┐
│                    Framework-Agnostic Design                           │
└───────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
        ┌───────────────────────┐       ┌───────────────────────┐
        │   Playwright Path     │       │   Selenium Path       │
        └───────────────────────┘       └───────────────────────┘
                    │                               │
                    ▼                               ▼
        ┌───────────────────────┐       ┌───────────────────────┐
        │  Browser.new_page()   │       │  webdriver.Chrome()   │
        └───────────────────────┘       └───────────────────────┘
                    │                               │
                    ▼                               ▼
        ┌───────────────────────┐       ┌───────────────────────┐
        │  PlaywrightAdapter    │       │  SeleniumAdapter      │
        │  (page)               │       │  (driver)             │
        └───────────────────────┘       └───────────────────────┘
                    │                               │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                        ┌───────────────────────┐
                        │    SmartLocator       │
                        │  (Same API for Both!) │
                        └───────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
        locator.click()                 locator.fill("text")
        locator.check()                 locator.hover()
        locator.select_option()         locator.drag_to()
                    │                               │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                        ┌───────────────────────┐
                        │   Web Application     │
                        └───────────────────────┘
```

---

## 📊 Data Flow - Form Interaction Example

```
User Test Code:
┌─────────────────────────────────────────────────────────────────┐
│  adapter = PlaywrightAdapter(page)                              │
│  username = SmartLocator("#username", adapter, "Username field")│
│  username.fill("john.doe")                                      │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
SmartLocator:
┌─────────────────────────────────────────────────────────────────┐
│  def fill(self, text):                                          │
│      return self._execute_with_healing(                         │
│          lambda: self.adapter.fill(self.locator, text)          │
│      )                                                           │
└─────────────────────────────────────────────────────────────────┘
                                │
                ┌───────────────┴───────────────┐
                │                               │
        Try #username                   Fails?
                │                               │
                ▼                               ▼
PlaywrightAdapter:                    AI Healer:
┌─────────────────────┐               ┌──────────────────────────┐
│  def fill():        │               │  • Analyze page HTML     │
│    element = page   │               │  • Extract context       │
│      .locator(      │               │  • Call AI (Groq)        │
│        "#username") │               │  • Get suggestions:      │
│    element.fill()   │               │    - input[name='user'] │
│                     │               │    - #user-input        │
│                     │               │    - .username-field    │
└─────────────────────┘               └──────────────────────────┘
                                                  │
                                    Try suggestions
                                                  │
                        ┌─────────────────────────┴───────────┐
                        │                                     │
                Found: input[name='user']          Update locator
                        │                                     │
                        ▼                                     ▼
            ┌─────────────────────┐             ┌──────────────────┐
            │  SUCCESS            │             │  username.locator│
            │  Text filled        │             │  = "input[name=  │
            │  Return result      │             │      'user']"    │
            └─────────────────────┘             └──────────────────┘
```

---

## 🎯 Locator Type Support Matrix

```
┌───────────────────────────────────────────────────────────────────────┐
│                      Supported Locator Types                           │
├───────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ✅ CSS Selectors           │  #id, .class, tag, [attr='value']       │
│  ✅ XPath                   │  //div[@id='test']                       │
│  ✅ ID                      │  id=username                             │
│  ✅ Name                    │  name=email                              │
│  ✅ Class                   │  class=btn-primary                       │
│  ✅ Tag                     │  tag=button                              │
│  ✅ Link Text               │  link=Home                               │
│  ✅ Partial Link            │  partial-link=Ho                         │
│  ✅ Text                    │  text=Submit                             │
│  ✅ Partial Text            │  text*=Sub                               │
│  ✅ ARIA Role               │  role=button[name='Submit']              │
│  ✅ Test ID                 │  data-testid=login-btn                   │
│  ✅ Title                   │  title=Click here                        │
│  ✅ Alt Text                │  alt=Logo image                          │
│  ✅ Placeholder             │  placeholder=Enter email                 │
│  ✅ Compound (Playwright)   │  button >> text=Submit                   │
│                                                                         │
│  🤖 AI Auto-Detection: SmartLocator automatically detects type!        │
│                                                                         │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 REST API Architecture

```
┌───────────────────────────────────────────────────────────────────────┐
│                     REST API Microservice                              │
│                  (services/locator_repair/)                            │
└───────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
                    ▼                               ▼
        ┌───────────────────────┐       ┌───────────────────────┐
        │   FastAPI Server      │       │   Python SDK          │
        │   (api.py)            │       │   (client.py)         │
        ├───────────────────────┤       ├───────────────────────┤
        │                       │       │                       │
        │  POST /repair         │◀─────▶│  repair_locator()     │
        │  {                    │       │                       │
        │    locator: str,      │       │  Usage:               │
        │    page_html: str,    │       │    client = Client()  │
        │    context_hint: str, │       │    result = client    │
        │    ai_provider: str   │       │      .repair_locator()│
        │  }                    │       │                       │
        │                       │       │                       │
        │  Response:            │       │  Returns:             │
        │  {                    │       │  {                    │
        │    success: bool,     │       │    suggestions: []    │
        │    suggestions: [],   │       │    confidence: float  │
        │    confidence: float  │       │  }                    │
        │  }                    │       │                       │
        │                       │       │                       │
        └───────────────────────┘       └───────────────────────┘
                    │                               │
                    └───────────────┬───────────────┘
                                    │
                                    ▼
                        ┌───────────────────────┐
                        │   AI Gateway          │
                        │   (ai_gateway.py)     │
                        ├───────────────────────┤
                        │  • Groq               │
                        │  • OpenRouter         │
                        │  • Gemini             │
                        │  • OpenAI             │
                        └───────────────────────┘
```

---

## 📦 Project Structure

```
ai_test_foundation/
│
├── core/
│   └── smart_locator/
│       ├── smart_locator.py          # SmartLocator class (40+ methods)
│       ├── smart_page.py             # SmartPage base class
│       ├── framework_adapter.py      # Adapters (800+ lines)
│       │   ├── FrameworkAdapter      # Base class (180 lines)
│       │   ├── PlaywrightAdapter     # Implementation (250 lines)
│       │   └── SeleniumAdapter       # Implementation (400 lines)
│       └── ai_healer.py              # AI healing engine
│
├── services/
│   ├── locator_repair/               # REST API microservice
│   │   ├── api.py                    # FastAPI server
│   │   ├── client.py                 # Python SDK
│   │   └── ai_gateway.py             # AI provider gateway
│   └── git_hooks/                    # Git integration
│
├── tests/
│   ├── test_ui_elements_comprehensive.py  # Complete test suite (600+ lines)
│   ├── test_locator_types_demo.py         # Locator type examples
│   └── test_smart_locator.py              # Unit tests
│
├── docs/
│   ├── COMPLETE_UI_ELEMENTS_GUIDE.md      # Complete guide (900+ lines)
│   ├── API_QUICK_REFERENCE.md             # Quick reference (400+ lines)
│   ├── LOCATOR_TYPES_GUIDE.md             # Locator guide (800+ lines)
│   ├── ENHANCEMENT_SUMMARY.md             # Enhancement summary
│   ├── FRAMEWORK_ARCHITECTURE.md          # This file
│   └── API_QUICK_START.md                 # REST API guide
│
└── config/
    └── config.yaml                        # Framework configuration
```

---

## 🚀 Execution Flow - Complete Test

```
1. Test Start
   │
   ├─▶ Create Browser/Driver
   │   │
   │   ├─▶ Playwright: browser = p.chromium.launch()
   │   │                page = browser.new_page()
   │   │
   │   └─▶ Selenium:   driver = webdriver.Chrome()
   │
2. Create Adapter
   │
   ├─▶ adapter = PlaywrightAdapter(page)
   │   OR
   └─▶ adapter = SeleniumAdapter(driver)
   │
3. Create SmartLocators
   │
   ├─▶ username = SmartLocator("#username", adapter, "Username")
   ├─▶ password = SmartLocator("#password", adapter, "Password")
   └─▶ submit = SmartLocator("button[type='submit']", adapter, "Submit")
   │
4. Execute Actions (with auto-healing)
   │
   ├─▶ username.fill("john.doe")
   │   │
   │   ├─▶ Try #username ✅ Success
   │   └─▶ Text filled
   │
   ├─▶ password.fill("secret")
   │   │
   │   ├─▶ Try #password ❌ Failed
   │   ├─▶ AI Healer activated
   │   ├─▶ New locator: input[type='password']
   │   └─▶ Retry ✅ Success
   │
   └─▶ submit.click()
       │
       ├─▶ Try button[type='submit'] ✅ Success
       └─▶ Form submitted
   │
5. Assertions
   │
   ├─▶ success_msg = SmartLocator(".success", adapter)
   └─▶ assert success_msg.is_visible() ✅ Pass
   │
6. Cleanup
   │
   └─▶ Close browser/driver
```

---

## 💡 Key Design Principles

### 1. **Adapter Pattern**
- Abstracts framework differences
- Same API for Playwright and Selenium
- Easy to add new frameworks

### 2. **AI-Powered Healing**
- Every method wrapped with healing logic
- Context-aware repair
- Learning from failures

### 3. **Comprehensive Coverage**
- 40+ methods for ALL UI types
- No custom helpers needed
- Production-ready

### 4. **Framework Agnostic**
- Write once, run anywhere
- Switch frameworks without code changes
- Consistent behavior

### 5. **Microservices Ready**
- REST API for locator repair
- Language-agnostic integration
- Scalable architecture

---

## 🎯 Success Metrics

| Metric | Value |
|--------|-------|
| Total Methods | 40+ |
| Framework Support | 2 (Playwright, Selenium) |
| Locator Types | 15+ |
| UI Element Types | 40+ |
| Auto-Healing Coverage | 100% |
| Documentation | 1,500+ lines |
| Test Coverage | 600+ lines |
| Code Size | 1,500+ lines |

---

**Architecture documented by: Ram, Senior AI Test Automation Engineer** 🏗️

**Complete, comprehensive, production-ready framework!** 🚀
